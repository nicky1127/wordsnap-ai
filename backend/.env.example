# Server Configuration
PORT=8080
NODE_ENV=development

# GCP Configuration
GCP_PROJECT_ID=wordsnap-ai
GCP_REGION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=../wordsnap-credentials.json

# Cloud Storage
GCS_BUCKET_NAME=wordsnap-ai-products

# Vertex AI Configuration
VERTEX_AI_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-1.5-flash-002

# Firebase (for authentication - we'll set this up in Phase 4)
# FIREBASE_PROJECT_ID=wordsnap-ai

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:5173
